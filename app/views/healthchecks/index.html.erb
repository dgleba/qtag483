
<row class="row is-table-row" >

  <div class="col-sm-1 ">
    <%= link_to 'Create', new_healthcheck_path , class: "btn btn-success"%>
  </div>

  <div class="col-sm-2 ">
    <h4>Listing Healthchecks</h4>
  </div>

  <div class="col-sm-3">
    <%= search_form_for @q, class: 'search-form' do |f| %>
      <div class="input-group">
        <%= f.search_field :name_cont, class: 'form-control' %>
        <span class="input-group-btn">
          <button class="btn btn-primary">Search <i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    <% end %>
  </div>

  <div class="col-sm-4 ">
    <%= paginate @healthchecks %>
  </div>
  
  <div class="col-sm-2 ">
    <%= page_entries_info @healthchecks, :entry_name => 'item' %>
  </div>
  
</row>


<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="1"></th>
            <th>Name</th>
      <th>Ip address</th>
      <th>Username</th>
      <th>User</th>
      <th>Comment</th>
      <th>Active status</th>
      <th>Sort order</th>
    </tr>
  </thead>

  <tbody>
    <% @healthchecks.each do |healthcheck| %>
      <tr>
        <td>
          <div class="btn-group">
          <%= link_to 'Show', healthcheck, class: "btn btn-info", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Edit', edit_healthcheck_path(healthcheck), class: "btn btn-primary", :style=>'color:#FFFFFF;' %>
          <% #commentedout  = link_to 'Destroy', healthcheck, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning", :style=>'color:#FFFFFF;' %>
          </div>
        </td>
        
        <td><%= healthcheck.name %></td>
        <td><%= healthcheck.ip_address %></td>
        <td><%= healthcheck.username %></td>
        <td><%= healthcheck.user %></td>
        <td><%= healthcheck.comment %></td>
        <td><%= healthcheck.active_status %></td>
        <td><%= healthcheck.sort_order %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
