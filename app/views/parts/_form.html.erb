
<%= form_for(@part, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @part.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@part.errors.count, "error") %> prohibited this part from being saved:</h4>
      <ul>
      <% @part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
            <%= f.label :Description, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.text_field :Description, class: "form-control" %>
      </div>
        </div>
  <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
        <div class="checkbox">
          <label>
            <%= f.check_box :RptScrap -%>
            Rptscrap
          </label>
        </div>
      </div>
      </div>
  <div class="form-group">
            <%= f.label :PlantNumber, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.number_field :PlantNumber, class: "form-control" %>
      </div>
        </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
