
<%= form_for(@tbl_customer, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @tbl_customer.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@tbl_customer.errors.count, "error") %> prohibited this tbl_customer from being saved:</h4>
      <ul>
      <% @tbl_customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
            <%= f.label :CustName, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.text_field :CustName, class: "form-control" %>
      </div>
        </div>
  <div class="form-group">
            <%= f.label :CustLocation, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.text_field :CustLocation, class: "form-control" %>
      </div>
        </div>
  <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
        <div class="checkbox">
          <label>
            <%= f.check_box :Current -%>
            Current
          </label>
        </div>
      </div>
      </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
