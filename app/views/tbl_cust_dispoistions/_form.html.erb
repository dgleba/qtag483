
<%= form_for(@tbl_cust_dispoistion, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @tbl_cust_dispoistion.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@tbl_cust_dispoistion.errors.count, "error") %> prohibited this tbl_cust_dispoistion from being saved:</h4>
      <ul>
      <% @tbl_cust_dispoistion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
            <%= f.label :Dispoisition, class: "col-sm-2 control-label" %>
        <div class="col-sm-10">
        <%= f.text_field :Dispoisition, class: "form-control" %>
      </div>
        </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
